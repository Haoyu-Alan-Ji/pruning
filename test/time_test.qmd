---
title: "multistart test"
author: "Haoyu Ji"
format: 
  html:
    self-contained: true
date: today
---

```{r setup, include=FALSE}
## path <- fs::dir_ls(here::here('test'), regexp = 'html')
## fs::file_move(path, here::here('file'))
knitr::opts_chunk$set(echo = TRUE)
library(here)
setwd(here::here())
```

```{r pkgs, message = FALSE, warning=FALSE}
library(Matrix)
library(ape)
library(tidyverse); theme_set(theme_bw())
library(expm)
library(RTMB)
```

```{r seed}
set.seed(427)
```

```{r funs}
source(here::here("R", "bestfit.R"))
```


```{r}
o1 <- sumfun(seed = 105, ntrait = 3, ntaxa = 20， multistart = TRUE, try.times = 20)
```